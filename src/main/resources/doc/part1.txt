1、重入
一个线程请求其他线程已经占有的锁时，请求将被阻塞。线程在试图获得它自己占有的锁时，请求会成功。

2、锁耗时
CPU通过时间片分配算法来循环执行任务，当前任务执行一个时间片后会切换到下一个任务。
但是，在切换前会保存上一个任务的状态，以便下次切换回这个任务时，可以再加载这个任务的状态。
所以任务从保存到再加载的过程就是一次上下文切换。

3、volatile
volatile变量在进行写操作时，会将当前处理器缓存行的数据写入内存，同时其他处理器里缓存了该内存地址的数据无效。

4、ThreadLocal
主要用来提供线程局部变量，也就是变量只对当前线程可见.